# Exercise 0.6 – New note in Single Page App

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a new note
    Note right of browser: User clicks Save

    Note right of browser: JavaScript prevents default form submission
    Note right of browser: New note added to browser memory

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of server: Server saves the note
    server-->>browser: HTTP 201 Created (JSON)
    deactivate server

    Note right of browser: Browser updates UI
    Note right of browser: No page reload happens
```
