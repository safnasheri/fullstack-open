sequenceDiagram
    participant Browser
    participant Server

    Note right of Browser: User submits note form
    Browser->>Browser: preventDefault()
    Browser->>Browser: Update UI (DOM)

    Browser->>Server: POST /new_note_spa (JSON data)
    activate Server
    Server->>Server: Save note
    Server-->>Browser: 201 Created
    deactivate Server

    Note right of Browser: Page stays the same (SPA behavior)
